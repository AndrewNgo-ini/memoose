{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "mnemoth": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "${PLUGIN_ROOT}", "mnemoth", "serve"],
      "env": { "MNEMOTH_DATA_DIR": "${PLUGIN_DATA}" }
    }
  }
}
