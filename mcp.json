{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "memoose": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "--from",
        "${PLUGIN_ROOT}",
        "memoose",
        "serve"
      ]
    }
  }
}
